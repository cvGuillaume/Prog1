public abstract class Cycle extends RoadVehicule
{
	// This programme defines a Cycle and is the abstract superclass of all the other cycles
	// Extension of the RoadVehicule class
	public Cycle(int maxSpeed)
	{
		// Defines the maximum speed of the cycle
		super(maxSpeed);
	}
}